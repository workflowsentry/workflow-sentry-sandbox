name: Excessive Permissions Scenario
on: [workflow_dispatch]
permissions: write-all
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "cenario: permissions write-all, deveria ser sinalizado"
